"""Nikita Heartbeat Engine package (Spec 215).

Phase 1 ships the math production module (:mod:`intensity`) and feature
flag (:func:`nikita.config.settings.heartbeat_engine_enabled`). Runtime
endpoints, planner, and pg_cron registration land in later PRs of the
Spec 215 sequence.

See `docs/models/heartbeat-intensity.md` for the model documentation
and `specs/215-heartbeat-engine/` for the SDD artifacts.
"""
