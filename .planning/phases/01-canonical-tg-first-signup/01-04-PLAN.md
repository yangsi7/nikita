---
phase: "01"
plan: "04"
type: execute
wave: 4
depends_on: ["01-02-PLAN.md", "01-03-PLAN.md"]
files_modified:
  - portal/src/app/auth/confirm/route.ts
  - portal/src/app/api/auth/dashboard_bridge/route.ts
  - nikita/api/routes/portal_auth.py
  - nikita/api/routes/telegram.py
  - nikita/api/routes/tasks.py
  - nikita/db/repositories/pending_registration_repository.py
  - nikita/db/repositories/__init__.py
  - nikita/db/dependencies.py
  - nikita/db/models/pending_registration.py
autonomous: true
requirements: [REQ-006, REQ-014, REQ-015, REQ-022]
exceeds_400_loc_guideline: "~700 lines — deletion-dominant PR (dashboard_bridge deleted, dead portal_auth endpoints removed, FSM PII log lines stripped, cleanup_pending_registrations endpoint + PendingRegistrationRepository shim deleted per REQ-022); solo-dev no-churn rule applies; do NOT split"

must_haves:
  truths:
    - "/auth/confirm route handles PKCE token_hash exchange only; ≤80 LOC; zero autobind side-effects"
    - "dashboard_bridge route deleted"
    - "portal_auth.py dead endpoints deleted or returning 410 (not 404)"
    - "All [LLM-DEBUG] log lines removed from nikita/ (rg returns 0 matches)"
    - "9 identified PII-leak log lines removed from telegram.py"
    - "cleanup_pending_registrations endpoint deleted from tasks.py; PendingRegistrationRepository file deleted; repo export removed from __init__.py and dependencies.py (REQ-022)"
    - "Pre-push gate: uv run pytest -q AND (cd portal && npm run test -- --run && npm run lint && npm run build) both green"
  artifacts:
    - path: "portal/src/app/auth/confirm/route.ts"
      provides: "Thin PKCE token_hash exchange ≤80 LOC; no autobind, no bridge calls"
    - path: "nikita/api/routes/telegram.py"
      provides: "Zero [LLM-DEBUG] lines; 9 PII-leak lines removed"
    - path: "nikita/api/routes/tasks.py"
      provides: "cleanup_pending_registrations function removed; file otherwise intact"
  key_links:
    - from: "portal/src/app/auth/confirm/route.ts"
      to: "Supabase exchangeCodeForSession or verifyOtp"
      via: "token_hash from query param only"
      pattern: "token_hash"
---

<objective>
PR-B: Endpoint deletions and PII cleanup. Strip /auth/confirm of autobind side-effects (thin PKCE handler ≤80 LOC), delete dashboard_bridge route, delete/410 dead portal_auth endpoints, remove all [LLM-DEBUG] and PII-leak log lines from the backend, and delete the cleanup_pending_registrations endpoint + PendingRegistrationRepository legacy shim (REQ-022: prevents pg_cron from firing against a dropped table in PR-C).

Purpose: Completes the portal-side and backend cleanup after the main Arch-B code landed in PR-D/PR-E. Removes surfaces that no longer serve a purpose, eliminates PII exposure in logs, and neutralises the pg_cron dead-path before the FSM tables are dropped.
Output: Thin /auth/confirm, deleted bridge, dead portal_auth endpoints gone, zero PII logs, cleanup endpoint + repo shim deleted.
</objective>

<execution_context>
@/Users/yangsim/.claude/get-shit-done/workflows/execute-plan.md
@/Users/yangsim/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-canonical-tg-first-signup/01-SPEC.md
@.planning/phases/01-canonical-tg-first-signup/01-RESEARCH.md
@.planning/phases/01-canonical-tg-first-signup/01-CONTEXT.md
@.planning/phases/01-canonical-tg-first-signup/01-02-SUMMARY.md
@.planning/phases/01-canonical-tg-first-signup/01-03-SUMMARY.md

<interfaces>
<!-- Thin /auth/confirm handler pattern (from SPEC.md FR-6) -->

/auth/confirm PKCE handler — what it does:
1. Read token_hash and type from query params
2. Read next from query params (default: /onboarding)
3. Call supabase.auth.verifyOtp({ token_hash, type }) OR exchangeCodeForSession
4. On success: redirect to `next` URL
5. On error: return 400 with error message
That is the ENTIRE route. No session init, no user_metadata write, no bridge call, no autobind.

Max 80 LOC for the entire route.ts file.

PII lines to remove (from SPEC.md FR-14/15 and RESEARCH.md Architectural Responsibility Map):
- All lines matching `[LLM-DEBUG]` in nikita/ directory
- Lines logging name, age, occupation, phone, email as raw values
- Specific location: telegram.py lines 577-695 region (FSM handler block removed in Plan 02)
  contains the majority of PII log lines. Plan 02 may have already removed these via FSM strip.
  Plan 04 grep-verifies and removes any survivors.
- Run: `rg -n "\[LLM-DEBUG\]" nikita/` — must return 0 after cleanup.
- Run: `rg -n "logger.*name.*age\|logger.*occupation\|logger.*phone" nikita/api/routes/telegram.py` — remove matches.

dashboard_bridge location: portal/src/app/api/auth/dashboard_bridge/route.ts (or similar path).
Verify with: `find portal/src -name "*bridge*" -o -name "*dashboard_bridge*"`
Delete entirely. Remove any imports of dashboard_bridge from other portal files.

portal_auth.py dead endpoints (from CONTEXT.md bulldoze list, FR-14):
Find in nikita/api/routes/portal_auth.py which endpoints are now dead after Arch-B.
Dead = were only called by /onboarding/auth (deleted in Plan 01) or by autobind flow (stripped).
Endpoints to assess: /portal/auth/autobind, /portal/auth/session, /portal/auth/link_telegram
(verify exact names by reading the file). For each dead endpoint: either delete the function
or return 410 if external callers might still hit it.

REQ-022 — cleanup endpoint + PendingRegistrationRepository (PARTIAL EDIT of tasks.py):
- tasks.py at nikita/api/routes/tasks.py: DELETE ONLY the `cleanup_pending_registrations` function
  and any imports that are unique to it. Leave ALL other functions in tasks.py intact.
- PendingRegistrationRepository at nikita/db/repositories/pending_registration_repository.py:
  The file docstring notes "The whole file is removed in PR-F3 ... that removal never happened."
  This is the PR-B deletion. Delete the entire file.
- nikita/db/repositories/__init__.py: Remove the export for PendingRegistrationRepository.
- nikita/db/dependencies.py: Remove any entry providing PendingRegistrationRepository.
- nikita/db/models/pending_registration.py: Only delete if `rg "PendingRegistration" nikita/ --type py`
  returns matches ONLY in this model file and the now-deleted repo file. If other live code
  still imports it, leave the model file and note in acceptance_criteria.
Rationale: After Plan 05 drops telegram_signup_sessions, pg_cron fires hourly against the
now-gone endpoint (tasks.py:681). Deleting the endpoint in PR-B means pg_cron gets 404
(non-fatal) rather than DB-error after the table drop. The cron job itself is unscheduled
atomically in PR-C (Plan 05) alongside the DROP TABLE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strip /auth/confirm to thin PKCE handler (≤80 LOC)</name>
  <files>portal/src/app/auth/confirm/route.ts</files>
  <read_first>
    - portal/src/app/auth/confirm/route.ts — current implementation with autobind side-effects
    - portal/src/app/auth/confirm/__tests__/route.test.ts — tests from Plan 03 (must pass after this task)
    - .planning/phases/01-canonical-tg-first-signup/01-PATTERNS.md — thin PKCE handler pattern
  </read_first>
  <behavior>
    - test: GET /auth/confirm?token_hash=abc&type=email&next=/onboarding → supabase verifyOtp called with {token_hash:"abc",type:"email"}, response redirects to /onboarding
    - test: GET /auth/confirm with no token_hash → 400 response
    - test: no call to any function matching "autobind", "bridge", "session_init", "update_user_metadata", "dashboard_bridge"
    - test: file line count ≤ 80
  </behavior>
  <action>
    Read portal/src/app/auth/confirm/route.ts (current implementation).
    Identify all autobind side-effects: calls to dashboard_bridge, session init, user_metadata updates, email verification webhook, any custom code beyond the token exchange.
    Rewrite the route to the thin pattern: read token_hash + type + next from query params; call supabase.auth.verifyOtp or exchangeCodeForSession; redirect to next on success; 400 on error.
    The route.test.ts from Plan 03 covers this — ensure all those tests now pass.
    Verify file is ≤80 LOC: `wc -l portal/src/app/auth/confirm/route.ts`
    If existing tests import autobind logic from confirm/route.ts, remove those import sites.
  </action>
  <verify>
    <automated>(cd portal && npm run test -- --run src/app/auth/confirm) && wc -l portal/src/app/auth/confirm/route.ts</automated>
  </verify>
  <acceptance_criteria>/auth/confirm route.ts is ≤80 LOC; route.test.ts from Plan 03 passes; no autobind side-effect calls in the file</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Delete dashboard_bridge + dead portal_auth endpoints + PII log cleanup</name>
  <files>
    portal/src/app/api/auth/dashboard_bridge/route.ts,
    nikita/api/routes/portal_auth.py,
    nikita/api/routes/telegram.py
  </files>
  <read_first>
    - nikita/api/routes/portal_auth.py — identify dead endpoints (autobind, session_link, link_telegram)
    - nikita/api/routes/telegram.py — find remaining [LLM-DEBUG] and PII log lines
    - portal/src/app/api/auth/dashboard_bridge/route.ts — confirm path before deletion
    - .planning/phases/01-canonical-tg-first-signup/01-PATTERNS.md — PII log cleanup patterns
  </read_first>
  <action>
    STEP A — dashboard_bridge deletion:
    Find the dashboard_bridge route file(s): `rg -l "dashboard_bridge" portal/src/`
    Delete the route file(s). Run `rg "dashboard_bridge" portal/src/` to find remaining imports — remove each import and any usage site. If a test file imports dashboard_bridge, delete or update the test.

    STEP B — portal_auth.py dead endpoint removal:
    Read nikita/api/routes/portal_auth.py. Identify endpoints that were part of the autobind / portal-link flow:
    - Any endpoint that was called exclusively by /auth/interstitial, /auth/bridge, or /onboarding/auth (all deleted)
    - Any endpoint with "autobind", "session_link", "link_telegram" in its name or route path
    For each dead endpoint: if it has a corresponding test — delete or 410 the test too.
    Return 410 for dead endpoints IF they are registered API routes that may be hit by cached clients.
    Delete entirely if no external caller could reasonably reach them post-Arch-B.
    Verify: `rg "autobind\|link_telegram\|interstitial" nikita/api/routes/portal_auth.py` returns 0 active function bodies.

    STEP C — PII log cleanup in telegram.py:
    Plan 02 removed lines 577-695 (FSM handler block). Verify: `rg -n "\[LLM-DEBUG\]" nikita/api/routes/telegram.py`
    Remove any remaining [LLM-DEBUG] lines. Also check: `rg -n "\[LLM-DEBUG\]" nikita/` for other files.
    Remove lines logging raw PII: name, age, occupation, phone as string format args.
    Use pattern: `rg -n "logger\.(info|debug|warning|error).*\.(name|age|occupation|phone)" nikita/api/routes/telegram.py`
    Remove matched lines. Replace with sanitized versions if the log intent matters (e.g., log user_id instead of name).
    Final check: `rg -n "\[LLM-DEBUG\]" nikita/` must return EMPTY.

    STEP D — Post-deletion import cleanup:
    `rg -l "portal_auth\|dashboard_bridge" portal/src/ nikita/` — check all importers.
    Remove dead imports. If a test file was exclusively testing deleted functionality, delete it.
    Ensure no circular import errors: `uv run python -c "import nikita.api.routes.portal_auth"` should work if file retained.
  </action>
  <verify>
    <automated>rg "\[LLM-DEBUG\]" nikita/ | wc -l && echo "above must be 0" && uv run pytest -q && (cd portal && npm run test -- --run && npm run lint && npm run build)</automated>
  </verify>
  <acceptance_criteria>
    `rg "\[LLM-DEBUG\]" nikita/` returns 0 lines;
    dashboard_bridge route deleted and no remaining imports;
    dead portal_auth endpoints removed or returning 410;
    uv run pytest -q green;
    portal test+lint+build green
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Delete cleanup_pending_registrations endpoint + PendingRegistrationRepository shim (REQ-022)</name>
  <files>
    nikita/api/routes/tasks.py,
    nikita/db/repositories/pending_registration_repository.py,
    nikita/db/repositories/__init__.py,
    nikita/db/dependencies.py,
    nikita/db/models/pending_registration.py
  </files>
  <read_first>
    - nikita/api/routes/tasks.py — locate cleanup_pending_registrations function (approx line 681); identify unique imports (if any) used only by this function
    - nikita/db/repositories/pending_registration_repository.py — confirm entire file is legacy shim; read its imports
    - nikita/db/repositories/__init__.py — find PendingRegistrationRepository export line
    - nikita/db/dependencies.py — find PendingRegistrationRepository provider entry
    - nikita/db/models/pending_registration.py — check if any live code outside repo shim imports this model
  </read_first>
  <action>
    Context: pg_cron fires `cleanup_pending_registrations` (tasks.py:681) hourly. Plan 05 (PR-C) drops
    `telegram_signup_sessions`. Deleting the endpoint here (PR-B) means pg_cron gets 404 (non-fatal)
    instead of a DB-level error in the window between PR-B and the cron being unscheduled in PR-C.
    The cron job itself is unscheduled atomically alongside DROP TABLE in Plan 05.

    STEP A — Locate and delete cleanup_pending_registrations in tasks.py:
    Read nikita/api/routes/tasks.py. Find the `cleanup_pending_registrations` function (expect near line 681).
    Also find any router registration line for this endpoint (e.g., `router.post("/tasks/cleanup_pending_registrations", ...)`).
    Delete ONLY: the function body + its decorator + its router registration line.
    Identify any imports at the top of tasks.py that are used EXCLUSIVELY by cleanup_pending_registrations
    (e.g., `from nikita.db.repositories import PendingRegistrationRepository`). Delete those import lines.
    Leave ALL other functions in tasks.py intact — this is a partial edit, not a file rewrite.

    STEP B — Delete PendingRegistrationRepository file:
    The file nikita/db/repositories/pending_registration_repository.py is a legacy shim per its docstring.
    Delete the entire file.

    STEP C — Remove export from __init__.py:
    Read nikita/db/repositories/__init__.py.
    Find the line exporting PendingRegistrationRepository (e.g., `from .pending_registration_repository import PendingRegistrationRepository`).
    Delete that line.

    STEP D — Remove from dependencies.py:
    Read nikita/db/dependencies.py.
    Find any dependency provider for PendingRegistrationRepository (e.g., `get_pending_registration_repository` or similar).
    Delete the function and its router include/annotation if present.

    STEP E — Conditional model file deletion:
    Run: `rg "PendingRegistration" nikita/ --type py --glob "!*pending_registration_repository.py"`
    If this returns ONLY nikita/db/models/pending_registration.py (and any test files now being deleted), delete nikita/db/models/pending_registration.py.
    If other live code (not being deleted in this PR) still imports PendingRegistration, leave the model file and note: "model file retained — live importer found at [path:line]".

    STEP F — Verify and run tests:
    `rg "PendingRegistrationRepository\|cleanup_pending_registrations\|pending_registration_repository" nikita/ --type py`
    Must return 0 matches (no remaining live references).
    `uv run pytest -q` — must pass.
    If any test file breaks because it imported PendingRegistrationRepository, delete or update that test file.
  </action>
  <verify>
    <automated>rg "PendingRegistrationRepository|cleanup_pending_registrations" nikita/ --type py | wc -l && echo "above must be 0" && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    cleanup_pending_registrations function and its router registration deleted from tasks.py; all other tasks.py functions intact;
    nikita/db/repositories/pending_registration_repository.py deleted;
    PendingRegistrationRepository export removed from __init__.py;
    PendingRegistrationRepository provider removed from dependencies.py;
    nikita/db/models/pending_registration.py deleted (or noted retained with live importer path if applicable);
    `rg "PendingRegistrationRepository|cleanup_pending_registrations" nikita/ --type py` returns 0;
    uv run pytest -q green
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /auth/confirm | PKCE token_hash from email link; only this route handles it |
| Log output → Cloud Run stdout | PII in log lines is observable to anyone with GCP log access |
| pg_cron → /tasks/cleanup_pending_registrations | Hourly cron job pointing to now-dead endpoint; must be neutralised before FSM table drop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Info-1 | Information Disclosure | [LLM-DEBUG] log lines exposing PII (name, age, occupation, phone) | mitigate | Task 2 Step C removes all [LLM-DEBUG] lines from nikita/. Final verification: `rg "\[LLM-DEBUG\]" nikita/` returns 0. Per REQ-014/REQ-015 / AC-13. |
| T-Spoof-2 | Spoofing | /auth/confirm could be abused to exchange arbitrary token_hash | mitigate | token_hash is signed by Supabase GoTrue; cannot be forged without the JWT secret. verifyOtp validates signature server-side. Thin handler does not introduce additional SSRF or redirect surface. |
| T-EoP-2 | Elevation of Privilege | autobind side-effects in /auth/confirm could grant unintended permissions | mitigate | Task 1 removes ALL code beyond token_hash exchange + redirect. Route.test.ts asserts no autobind call sites remain. |
| T-DoS-3 | Denial of Service | pg_cron fires cleanup_pending_registrations hourly after telegram_signup_sessions is dropped in PR-C | mitigate | Task 3 deletes the endpoint in PR-B. After PR-B deploys, pg_cron fires 404 (non-fatal) instead of a DB exception. Plan 05 unschedules the cron job atomically alongside DROP TABLE — no persistent error window. Per REQ-022. |
</threat_model>

<verification>
## Wave 4 Gate Checks (run before PR-B merge)

```bash
# 1. Zero LLM-DEBUG lines
rg "\[LLM-DEBUG\]" nikita/ | wc -l
# Must be 0

# 2. auth/confirm is thin
wc -l portal/src/app/auth/confirm/route.ts
# Must be ≤ 80

# 3. dashboard_bridge deleted
ls portal/src/app/api/auth/dashboard_bridge/ 2>/dev/null && echo "FAIL: still exists" || echo "OK: deleted"

# 4. No remaining autobind imports
rg "autobind\|dashboard_bridge" portal/src/ nikita/ --type ts --type py | grep -v "\.test\.\|spec\." | head -20

# 5. auth/confirm tests pass
(cd portal && npm run test -- --run src/app/auth/confirm)

# 6. REQ-022: cleanup endpoint + repo shim deleted
rg "PendingRegistrationRepository|cleanup_pending_registrations" nikita/ --type py | wc -l
# Must be 0

# 7. Full pre-push gates
uv run pytest -q
(cd portal && npm run test -- --run && npm run lint && npm run build)
```
</verification>

<success_criteria>
- /auth/confirm route.ts ≤80 LOC with zero autobind side-effects
- dashboard_bridge route deleted, zero remaining imports
- Dead portal_auth endpoints deleted or returning 410
- `rg "[LLM-DEBUG]" nikita/` returns 0 lines
- cleanup_pending_registrations endpoint deleted from tasks.py; PendingRegistrationRepository file deleted; __init__.py + dependencies.py exports removed (REQ-022)
- All portal tests (including route.test.ts from Plan 03) pass
- `uv run pytest -q` passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-canonical-tg-first-signup/01-04-SUMMARY.md` using the summary template. Include: auth/confirm LOC count, count of PII lines removed, count of dead endpoints removed, dashboard_bridge deletion confirmation, PendingRegistrationRepository deletion confirmation, model file retention decision (deleted or retained + reason), pre-push gate results.
</output>
