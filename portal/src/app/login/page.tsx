import type { Metadata } from "next"
import LoginPage from "./page-client"

export const metadata: Metadata = {
  title: "Log In | Nikita",
  description: "Sign in to your Nikita account",
}

export default LoginPage
