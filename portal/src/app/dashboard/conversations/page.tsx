import type { Metadata } from "next"
import ConversationsPage from "./page-client"

export const metadata: Metadata = {
  title: "Conversations | Nikita",
  description: "View your conversation history with Nikita",
}

export default ConversationsPage
