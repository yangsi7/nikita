import type { Metadata } from "next"
import SettingsPage from "./page-client"

export const metadata: Metadata = {
  title: "Settings | Nikita",
  description: "Manage your account and preferences",
}

export default SettingsPage
