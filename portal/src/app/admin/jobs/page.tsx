import type { Metadata } from "next"
import JobsPage from "./page-client"

export const metadata: Metadata = {
  title: "Background Jobs | Nikita",
  description: "Monitor background job execution",
}

export default JobsPage
