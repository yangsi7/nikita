import type { Metadata } from "next"
import PipelinePage from "./page-client"

export const metadata: Metadata = {
  title: "Pipeline Health | Nikita",
  description: "Monitor pipeline processing status",
}

export default PipelinePage
