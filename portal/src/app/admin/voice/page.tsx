import type { Metadata } from "next"
import VoiceMonitorPage from "./page-client"

export const metadata: Metadata = {
  title: "Voice Sessions | Nikita",
  description: "Monitor voice call sessions",
}

export default VoiceMonitorPage
