import type { Metadata } from "next"
import AdminUserDetailPage from "./page-client"

export const metadata: Metadata = {
  title: "User Detail | Nikita",
  description: "View player details and metrics",
}

export default AdminUserDetailPage
